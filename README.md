# Mermaid code rendering in a quarto document - a test.

I was trying out how mermaid graphics were rendered in quarto documents, and I tested this with different configurations and output formatd (`html`, `pdf`, and github markdown).
